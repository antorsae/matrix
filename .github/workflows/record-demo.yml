# Demo recording workflow - disabled due to CI complexity
# To update the demo.gif, record locally using:
#   asciinema rec -c "python matrix.py" demo.cast
#   agg demo.cast demo.gif
# Or use VHS locally:
#   vhs demo.tape
# Then commit the demo.gif manually.

name: Record Demo GIF

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Demo recording requires local execution. See workflow comments."
